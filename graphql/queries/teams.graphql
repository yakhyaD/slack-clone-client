query Teams {
  teams {
      teamsOwned {
        id
        name
        ownerId
        channels {
        id
        name
      }
    }
    teamsInvited {
      team {
        id
        name
        ownerId
        channels {
          id
          name
        }
      }
    }
  }
}
